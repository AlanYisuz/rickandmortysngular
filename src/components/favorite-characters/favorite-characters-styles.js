import { css } from "lit-element";

export default css`

    .card-container {
    display: flex;
    justify-content: center;
    align-items: center;
    display: flex;
    flex-wrap: wrap;
    gap: 20px; /* Espacio entre cards */
    padding: 20px;
  }`